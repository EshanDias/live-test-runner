{
  "name": "@live-test-runner/core",
  "version": "1.0.0",
  "description": "Core logic for Live Test Runner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@live-test-runner/runner": "workspace:*"
  },
  "devDependencies": {
  }
}